# LogRocket — public app id, safe to expose in the client bundle.
VITE_LOGROCKET_APP_ID=your-org/your-app

# Set to "true" to enable LogRocket during local dev. Defaults to off.
VITE_LOGROCKET_ENABLE_IN_DEV=false
